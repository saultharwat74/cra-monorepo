import React from 'react';
import styles from './styles.module.sass'
const OrderingMethods: React.FC = () => {
    return <div className={styles.wrapper}>
        ordering Met
    </div>
};

export default OrderingMethods